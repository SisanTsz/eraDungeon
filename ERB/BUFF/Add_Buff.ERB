;----------------------------------------------------------------
; TODO: 为角色添加一个buff
;----------------------------------------------------------------
@Add_Buff(caster_ID, target_ID, buff_ID, duration, isSetDuration = FALSE, stack, isSetStack = FALSE)
    #DIM caster_ID
    #DIM target_ID
    #DIM buff_ID
    #DIM duration
    #DIM isSetDuration
    #DIM stack
    #DIM isSetStack

    RETURN
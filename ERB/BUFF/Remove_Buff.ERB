;----------------------------------------------------------------
; TODO: 移除一个buff，同时触发移除buff事件
;----------------------------------------------------------------
@Remove_Buff(buff_ID, reason = "自身效果")
    #DIM buff_ID
    #DIMS reason

    RETURN
;----------------------------------------------------------------
; 获取角色身上所有buff ID作为待解析字符数组返回
;----------------------------------------------------------------
@Get_BuffList_From_Chara(chara_ID)
#FUNCTION
    #DIM chara_ID
    ; 有没有更好的写法呢，era又没有List.Add()
    #DIMS buff_List_String

    buff_List_String =
    REPEAT VARSIZE(BUFF_TARGET)
        IF BUFF_TARGET:COUNT == chara_ID
            buff_List_String += TOSTR(COUNT) + ","
        ENDIF
    REND
    RETURNF buff_List_String
;----------------------------------------------------------------
; 数组相关方法
;----------------------------------------------------------------
;----------------------------------------------------------------
; 解析words为数组，然后搜索有无element，返回一个序号，-1则为空
;----------------------------------------------------------------
@Arrays_Contains(words, element, completeMatch = 1)
#FUNCTION
    #DIMS words
    #DIMS element
    #DIM completeMatch

    SPLIT words ",", LOCALS
    RETURNF FINDELEMENT(LOCALS, element, , , completeMatch)